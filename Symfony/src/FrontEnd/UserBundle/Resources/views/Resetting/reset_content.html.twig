{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('fos_user_resetting_reset', {'token': token}) }}" {{ form_enctype(form) }} method="POST" class="fos_user_resetting_reset form-signin">
    <h2 class="form-signin-heading">Bienvenue,<br />
        <p class="text-center"> <small>Réinitialiser votre mot de passe,</small></p>
    </h2>
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'resetting.reset.submit'|trans }}" class="btn-danger"  />
    </div>
</form>
