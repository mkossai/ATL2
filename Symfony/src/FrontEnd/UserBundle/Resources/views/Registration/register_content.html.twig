{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register" id="show_register" >
    {{ form_widget(form) }}
    <div>
        <br /><input type="submit" value="{{ 'registration.submit'|trans }}" class="btn-danger" /><a href="{{path('CoreBundle_homepage')}}" class="btn" style="margin-left:40px;"> Annuler</a>
    </div>
</form>
