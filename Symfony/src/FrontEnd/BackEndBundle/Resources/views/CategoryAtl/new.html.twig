{% extends '::admin.html.twig' %}

{% block body -%}
    <h1>CategoryAtl creation</h1>

    <form action="{{ path('categoryatl_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('categoryatl') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
